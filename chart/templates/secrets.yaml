{{- range $key, $value := .Values.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.name }}-{{ $key | lower }}-secret
type: Opaque
data:
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}